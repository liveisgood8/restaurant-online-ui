import React from 'react';
import { MenuContainer } from './features/MenuContainer';

const App: React.SFC = () => {
  return (
    <React.Fragment>
      <MenuContainer />
    </React.Fragment>
  );
}

export default App;
